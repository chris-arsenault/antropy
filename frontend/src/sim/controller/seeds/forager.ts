/**
 * GENERATED ARTIFACT (ADR-0010): baked founder weights derived by the
 * harness ES pipeline (`pnpm harness derive`). Null: five derivations
 * (ledger runs 23-31) aced the probe suite and failed in vivo — the
 * probe-proxy gap is a recorded finding; seed() uses the hand-derived
 * backbone until derivation against in-vivo fitness lands (backlog).
 * Do not edit by hand — regenerate.
 */
export const FORAGER_SEED: number[] | null = null;
