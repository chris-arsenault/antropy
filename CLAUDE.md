@AGENTS.md

## Claude-specific

- Documentation surface (README, AGENTS.md, docs/, ADRs, backlog, changelog) follows the
  `repo-docs` skill conventions — use it when creating or restructuring docs.
- Non-trivial feature work starts with the `feature-start` skill; expand individual phases
  with `plan-phase` before executing them.
