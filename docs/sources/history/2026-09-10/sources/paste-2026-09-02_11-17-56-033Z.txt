Authorized — and before the edits, two things the agent has earned. First, an explicit retraction: "amplify doing double duty" was my claim, stated confidently, and the measurement falsified it. The geometry is the mechanism's blind spot, not an implementation gap: stereo differential steering reads the cross-axis component of a gradient, and a source directly behind the ant puts the entire gradient along the body axis — both forward samples equal, differential zero, no turn, forever. Pure stereo taxis is structurally blind to "behind." The agent is right that keeping the double-duty language would hide an absent mechanism, and right to refuse certification over it.

Second, its arithmetic contains a diagnosis it didn't quite state, worth handing back: 9 spoil cycles, shaft deepened by 1. Those other ~8–17 dug voxels went somewhere — either scattered surface divots (unloaded ant with dig pinned high excavates wherever it stands the moment it's emptied) or straight back into the shaft mouth (loaded ant deposits into the nearest air, refilling its own work). Both are the same root cause as the return failure: the seed as specified has no way to distinguish the dig site from a dig site, and no way to distinguish "clear of the shaft" from "in it." So the amendment needs to be slightly larger than "add reacquisition" — three gating terms, all still linear and readable:

Reflex 5, reacquisition-by-casting: total signal low (A_L + A_R below threshold) → constant turn rate. The lost ant orbits until its nose crosses the gradient's cross-axis, then reflex 3 takes over. This is the reflex-grade form of the behavior — real insects do exactly this casting when they lose a plume — and it needs no memory. (The temporal-derivative alternative, klinokinesis, needs a leaky accumulator = hidden state = not seed-legal; if casting proves insufficient, that is the CMA-ES escalation already in the ladder, not a hand-written workaround.)
Dig gated by local A (the dig-face half of amplify finally made load-bearing): dig fires where trail concentration is high, stays quiet where it's low. Kills divot spam.
Deposit anti-gated by A: deposit requires loaded ∧ low local A — "dump where the trail isn't," which is both the linear-threshold version of the oracle's "dump clear" and the correct stigmergic inversion. Kills shaft-refilling, no conjunction products needed.

Gating dig by A creates one bootstrap hole — at t=0, A is zero everywhere and nothing ever digs. The scenario fix: the step-8 fixture pre-marks the intended column's mouth with channel A (O-layer scenario setup, fully legal), and the forward-looking note is that this becomes "founding marks the dig site" at colony scale — the scripted founding chamber deposits initial A, which is legal by the same fiat that scripts the chamber itself.

On the weight budget: granted, restated by purpose rather than number. The 2–4 figure was a readability heuristic, not a load-bearing constraint — the binding requirements are that the seed stays zero-hidden-state, linear-threshold, hand-verifiable locus by locus, and rung-3 assayable. Per-relay 2–4 with a total seed budget around 20 loci satisfies all of that.

Applying the amendments to the canonical appendix now (the repo copy should sync from this):

Appendix updated. The authorization to relay, in the agent's terms:

Amendment approved with modifications. Five reflexes, not the four-plus-reacquisition it proposed — because its own measurements (9 cycles, +1 voxel) imply two more absent gates beyond return: the dug voxels that didn't deepen the shaft went either into surface divots (emptied ant, dig pinned high, digs where it stands) or back into the shaft mouth (loaded ant deposits into nearest air). So the amended step 5 specifies: casting as the reacquisition mechanism (total-A low → constant turn; klinokinesis is memory and stays seed-illegal — if casting can't close it, the escalation is the ladder's existing CMA-ES path, not a cleverer hand-written mechanism), dig gated by local A (kills divot spam), and deposit anti-gated by A (dump-where-the-trail-isn't — kills refilling, no conjunction products required). Plus the bootstrap consequence: the step-8 fixture pre-marks the column mouth with channel A, O-layer scenario setup, with "founding marks the dig site" noted as the colony-scale future of the same move.

Weight budget granted as restated: the constraint is readability — zero hidden state, linear-threshold, verifiable locus by locus, 2–4 per relay, ~20 total. The old per-reflex number was heuristic and is gone.

The falsified claim is recorded in the document itself — step 5's row now carries the retraction and the geometric reason (stereo taxis reads only the cross-axis gradient component; "behind" is invisible by construction), so the failed assumption can't silently regenerate in a future edit. Step 12's future carry-to-target became a "sixth reflex" with both amendments cited as the template.

It should resume at step 6 with the five-reflex weight set, re-run the rung-3 assays (casting and the two gates each need their own synthetic-stimulus test — casting in particular: uniform-low A → turning; strong ahead-A → no casting override of taxis), fix the pre-mark into the fixture, and then attempt step-8 recertification. And once it's green, the repo's Appendix D copy should be synced from this canonical one rather than diverging — two drifting copies of the governing document is exactly the disease the drift rule in §D.3.3 exists for.

One prediction worth logging before the run, in the predict-then-measure spirit: casting plus taxis should produce visibly orbital return paths — the ant overshooting, circling, and spiraling in rather than beelining. If the trajectory looks like that, the mechanism is working as designed; if it beelines, something else is steering and that's worth a look even though the test passes.