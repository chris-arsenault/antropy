//! Retired displacement/refitting experiment; recover its code from the historical commit.
fn main() -> Result<(), Box<dyn std::error::Error>> {
    Err("Historical displacement experiment retired: use its original commit and schema; current cells have birth-fixed capabilities.".into())
}
