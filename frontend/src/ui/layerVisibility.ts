export interface LayerVisibility {
  readonly temperature: boolean;
  readonly moisture: boolean;
  readonly routes: boolean;
  readonly foodOdor: boolean;
  readonly nestOdor: boolean;
  readonly pheromoneA: boolean;
  readonly pheromoneB: boolean;
  readonly freshAir: boolean;
}
